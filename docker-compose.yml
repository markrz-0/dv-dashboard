services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: glottoscope_dashboard
    ports:
      - "8050:8050"
    restart: unless-stopped
    volumes:
      # Optional: mount data directory for easy updates without rebuild
      - ./data:/app/data
